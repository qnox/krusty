fun calc(
    a: Int,
    b: Int,
): Int {
    val sum = a + b
    return sum * 2
}

fun single(a: Int) = a

class C(
    a: Int,
    b: Int,
)

fun String.g(
    a: Int,
    b: Int,
): Int = a + b.length

val h = { x: Int, y: Int -> x + y }
