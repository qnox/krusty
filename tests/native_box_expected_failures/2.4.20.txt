# Corpus cases (Kotlin 2.4.20) the native lane ACCEPTS and runs WRONG today, each a known defect outside the native
# backend that the JVM lane fails on as well. Format: <path relative to the box corpus root><TAB><reason>.
# A case not listed here that fails, or a case listed here that passes, fails the test — so this list
# can only shrink honestly. See tests/kotlin_box_native_conformance.rs.
callableReference/property/extensionPropertyReferenceWithTypeParameter.kt	`List<T>::foo` names a LOCAL extension function `foo` and the checker does not see it there, so the reference falls through to the top-level extension PROPERTY of the same name and the program answers "not" where it should answer "led" — reduced to that one reference, the checker reports `unresolved reference 'foo'`; krusty's JVM backend prints the identical "fail", so the resolution is the checker's, not the generator's
classes/selfcreate.kt	an anonymous object passes its enclosing instance's `b` to its own superclass constructor; krusty's JVM backend rejects the same program with `VerifyError: Bad type on operand stack`
companionBlocksAndExtensions/initializersExtensions.kt	answers "FAIL: initialized=true" — a known failure of the JVM lane for 2.4.20 as well (tests/box_expected_failures/2.4.20.txt), so the defect is in the shared frontend or lowering, not the generator
contextParameters/classMemberAndTopLevelWithContext.kt	a context parameter picks the wrong receiver — krusty's JVM backend prints the identical "NOK", so this is the frontend's, not the generator's
contextParameters/sameExtensionContextAndDispatchReceiver.kt	`this@A` inside a member extension resolves to a CONTEXT PARAMETER of the same type instead of the dispatch receiver, so `context(A("c ")) { A("e ").funMember() }` answers "c c e " where kotlinc answers "c d e " — krusty's JVM backend prints the identical "NOK", so the receiver choice is the checker's, not the generator's
defaultArguments/convention/incWithDefaultInGetter.kt	`++` on a property whose getter takes a defaulted argument counts the accessor calls wrongly — krusty's JVM backend prints the identical "fail 2: anone1"
defaultArguments/function/kt36188.kt	a class inheriting the same defaulted method from two interfaces takes the wrong interface's default ("Fail" instead of "OK") — krusty's JVM backend prints the identical "Fail", so the choice is common lowering's
defaultArguments/function/kt36188_2.kt	same inherited-default choice as defaultArguments/function/kt36188.kt, through one more interface; krusty's JVM backend prints the identical "Fail"
evaluate/intrinsicConst/enumNameWithInit.kt	`EnumClass.OK.name` under +IntrinsicConstEvaluation is meant to fold at compile time, so the enum's `init` never runs; krusty evaluates it at run time and the `init` sets "Fail" — krusty's JVM backend prints the identical "Fail", so the folding is the frontend's to do
extensionFunctions/executionOrder.kt	an extension call evaluates its receiver and function in the wrong order — krusty's JVM backend prints the identical "fail getReceiver->getFun()->End"
fir/functionsDifferInTypeParameterBounds.kt	two functions differing only in their type-parameter BOUNDS are one overload set to krusty, so the wrong one is selected and `box` prints `KK`; krusty's JVM backend rejects the same program with `VerifyError: Bad type on operand stack`, so the selection is the frontend's
fullValueClasses/interopWithInlineClasses.kt	a +FullValueClasses value class of two fields, nested in single-field ones: the shared lowering does not model multi-field value classes, and the JVM lane lists this case as a known failure for 2.4.20 too; natively a `require` fails
fullValueClasses/lateinit.kt	a `lateinit` +FullValueClasses value class of two fields: the shared lowering does not model multi-field value classes, and the JVM lane lists this case as a known failure for 2.4.20 too; natively the program crashes (SIGSEGV)
function/defaultsWithVarArg2.kt	a `vararg` parameter's DEFAULT is not applied — `foo()` on `fun foo(vararg arr: Int = intArrayOf(1, 2))` walks an empty array, so the case answers `Expected <1\n2\n42\n>, actual <42\n>`; krusty's JVM backend reports the identical AssertionError, so the missing default is common lowering's rather than this generator's
ieee754/smartCastOnWhenSubjectAfterCheckInBranch_properIeeeComparisons.kt	a `when` subject narrowed by an earlier `!is Float ->` arm still compares through `Any.equals` in the later arms, so `-0.0F` does not match `0.0F`; the checker reads the subject's type once, before any arm narrows it. krusty's JVM backend prints the identical "Fail 1: other". Being fixed in the frontend rather than here — remove this line with that fix
increment/argumentWithSideEffects.kt	`++getA().x` must re-READ the property after storing it, so the log ends `set-A.x;get-A.x;`; krusty stops at the store. krusty's JVM backend prints the identical `ERROR:AssertionError:++getA().x expectedLog: getA();get-A.x;set-A.x;get-A.x;, actual: getA();get-A.x;set-A.x;`, so the missing re-read is common lowering's
inference/eagerLambdaAnalysisSamAndLambda.kt	answers "OO" — a known failure of the JVM lane for 2.4.20 as well (tests/box_expected_failures/2.4.20.txt), so the overload the frontend picks is shared, not the generator's
inference/whenAtAssignment.kt	a `when` assigned to an interface-typed `val` takes the type of one branch, so the other branch's value is cast to it — krusty's JVM backend throws the identical `ClassCastException: class A cannot be cast to class B`, and the case itself records kotlinc's K2 failing the same way (KT-77008)
inference/whenAtAssignmentWithSmartCast.kt	same `when`-assignment shape as inference/whenAtAssignment.kt, with a smart cast; krusty's JVM backend throws the identical `ClassCastException`
inference/whenAtAssignmentWithSmartCastInsideCapturedLambda.kt	same `when`-assignment shape as inference/whenAtAssignment.kt, inside a captured lambda; krusty's JVM backend throws the identical `ClassCastException: class A cannot be cast to class B`
localClass/virtualCallFromConstructor.kt	a local class's constructor calls an overridden method that writes a captured `var`, and the capture field is written AFTER the superclass constructor runs — the same store order as super/kt4982.kt. krusty's JVM backend throws `NullPointerException: Cannot assign field "element" because "this.$result" is null` on the same program, so the order is common lowering's
localClasses/localClassInParameterInitializer.kt	the compiler panics before any backend runs (`Pass 2 must consume every checked signature default`, src/compiler.rs): a local class declared inside a default argument; the JVM lane lists the same case in tests/box_expected_failures
multiDecl/nameBasedDestructuringUnderscoreWithRenaming.kt	a name-based destructuring with a renamed underscore binds the wrong component — krusty's JVM backend prints the identical "FAIL: 0", so this is the frontend's
nothingValue/nothingValueException.kt	a callable whose result type is `Nothing` must raise `KotlinNothingValueException` where it returns anyway; krusty raises neither backend's, and its JVM backend prints the identical `Should fail with exception`
objects/flist.kt	`FList.reverse` recurses without end: the walk stops at `empty`, and the anonymous `object : FList<T>()` a `plus` builds answers the enclosing list for its `tail` and the captured parameter for its `head`, so a chain that should shorten never does. krusty's JVM backend fails the identical program with `StackOverflowError`, so the recursion is the frontend's rather than this generator's; on native the same unbounded recursion is a SIGSEGV, there being no guard-page handler to name it
objects/kt3684.kt	`n` inside an anonymous subclass of `X` names the ENCLOSING `X` instance's private field, not the anonymous instance's own; both backends read the anonymous instance's, so krusty's JVM backend prints the identical `inner`
privateConstructors/inner.kt	`this@Outer` inside an `inner` class that also EXTENDS its outer resolves to `this` rather than the enclosing instance, so `this.s + this@Outer.s` answers "OO" instead of "OK" — krusty's JVM backend prints the identical "OO", so the receiver is the frontend's
properties/fieldInsideField.kt	a property's `get() = field + object : Your() { override val your = "K" get() = field }.foo()` — the inner `field` binds to the outer property in both backends, so krusty's JVM backend fails the same program with `StackOverflowError`
properties/lateinit/topLevel/uninitializedMemberAccess.kt	the same missing top-level `lateinit` guard as properties/lateinit/topLevel/uninitializedRead.kt, reached through a member call on the unset property: krusty's JVM backend gets a `NullPointerException` off the same null where kotlinc raises `UninitializedPropertyAccessException`, and this target dereferences it
properties/lateinit/topLevel/uninitializedRead.kt	a TOP-LEVEL `lateinit var`'s getter carries no throw-if-null guard, so reading one before it is assigned answers null instead of `UninitializedPropertyAccessException`; a lateinit MEMBER is guarded (the field read carries it) and krusty's JVM backend prints the identical `Should throw an exception`, so the missing guard is common lowering's
reflection/classes/qualifiedName.kt	a LOCAL class has no `qualifiedName` in Kotlin and krusty answers one anyway; its JVM backend fails the identical assertion (`Expected <null>, actual <test.box$Local>`), so the name is the frontend's to withhold rather than this runtime's
reflection/noReflectAtRuntime/methodsFromAny/delegatedProperty.kt	a `KProperty`'s `toString` must read `property x (Kotlin reflection is not available)` and this runtime renders the object instead; krusty's JVM backend does not get that far, rejecting the same program with `VerifyError: Operand stack underflow`
safeCall/parenthesizedSafeCallsAndOperators.kt	KT-68834: a safe call's receiver narrows to non-null for OPERATOR selection, so `arg?.alias[42]` takes the member `get` while the parenthesized `(arg?.alias)[42]` takes the `Foo?` extension; krusty picks the extension for both and the case reports `fail: get: member, but get: extension`. krusty's JVM backend prints the identical `ERROR:RuntimeException:fail: get: member, but get: extension`, so the selection is the frontend's, not the generator's
sam/constructors/sameWrapperClass2.kt	two SAM conversions of the SAME lambda to the same `fun interface` must be instances of ONE wrapper class, and both backends emit one type per conversion SITE — so `(SAM(f) as Any)::class` answers a different class each time. krusty's JVM backend prints the identical failure, with two `$$Lambda` classes of its own, so the one-type-per-site rule is common lowering's and not the native generator's
sam/topLevelConversions.kt	crashes (SIGSEGV) under +ResolveTopLevelLambdasAsSyntheticCallArgument, where a lambda is stored straight into a `fun interface` type — a known failure of the JVM lane for 2.4.20 as well (tests/box_expected_failures/2.4.20.txt): the frontend does not model the conversion, and this target calls the lambda through the interface slot it does not have
smartCasts/invokeRecieverSmartcastK2.kt	`(a as String)(a)` selects `String.invoke(Any)` where kotlinc selects `Any.invoke(String)` — krusty's JVM backend prints the identical "FAIL: implicitReceiverCast(): String.invoke(Any)", so the selection is the frontend's
super/kt4982.kt	an abstract class's `init` calls an overridden method that writes a captured `var`, and the anonymous subclass's capture field is written AFTER the superclass constructor runs — krusty's JVM backend throws `NullPointerException: Cannot assign field "element" because "this.result" is null` on the same program, so the store order is common lowering's
try/try5.kt	answers "0" — a known failure of the JVM lane for 2.4.20 as well (tests/box_expected_failures/2.4.20.txt), so the defect is in the shared frontend or lowering, not the generator
vararg/kt10926.kt	a candidate taking a VARARG of a more specific type loses to one taking a bare type parameter or `Any`: `fun addAll7(vararg values: Collection<T>)` beside `fun addAll7(values: T)` must select the vararg for `addAll7(listOf(""))` under +EliminateAmbiguitiesWithExternalTypeParameters, and krusty selects the other, so the case answers "NOK" — krusty's JVM backend prints the identical "NOK", so the specificity comparison is the frontend's
when/whenSubjectVariable/ieee754EqualityWithSmartCast.kt	the same IEEE 754 rule as binaryOp/eqNullableDoublesWithTP.kt, reached through a `when` subject variable that is smart-cast; krusty's JVM backend fails the identical assertion (`ERROR:AssertionError:null`)
