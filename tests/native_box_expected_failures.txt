# Corpus cases the native lane ACCEPTS and runs WRONG today, each a known defect outside the native
# backend that the JVM lane fails on as well. Format: <path relative to the box corpus root><TAB><reason>.
# A case not listed here that fails, or a case listed here that passes, fails the test — so this list
# can only shrink honestly. See tests/kotlin_box_native_conformance.rs.
casts/kt83324.kt	the frontend smart-casts a `var` that the loop reassigns, so the IR carries a cast to the first assignment's class; the cast fails at runtime (a frontend defect, not a lowering one)
diagnostics/functions/tailRecursion/extensionTailCall.kt	common lowering turns only top-level non-extension `tailrec` functions into loops; an extension `tailrec` recurses a million deep and overflows the stack
diagnostics/functions/tailRecursion/infixCall.kt	common lowering turns only top-level non-extension `tailrec` functions into loops; an infix extension `tailrec` recurses a million deep
diagnostics/functions/tailRecursion/infixRecursiveCall.kt	common lowering turns only top-level non-extension `tailrec` functions into loops; an infix extension `tailrec` recurses a million deep
diagnostics/functions/tailRecursion/recursiveInnerFunction.kt	common lowering does not loop-transform a local `tailrec` function; it recurses a million deep
diagnostics/functions/tailRecursion/thisReferences.kt	common lowering does not loop-transform a member `tailrec` function; it recurses a million deep
diagnostics/functions/tailRecursion/unitBlocks.kt	common lowering loops only `return f(...)` tail calls; a Unit-typed tail call without `return` stays a call and recurses a million deep
intrinsics/prefixIncDec.kt	`++prop` on a property with custom accessors reads it once where kotlinc reads it twice, so the accessor side effect is counted 2 instead of 3 — krusty's JVM backend prints the identical "fail in prefix increment: 2 != 3", so this is common lowering's, not the generator's
