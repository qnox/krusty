target/
dist/
krusty-out/
docs/badges/
# LLVM source-based coverage counter dumps (see scripts/coverage.sh); can land in the tree root if a
# coverage-instrumented binary runs without LLVM_PROFILE_FILE pointed at target/.
*.profraw
# cargo-mutants run output
mutants.out/
dhat-heap.json
.idea
.DS_Store
__pycache__/
*.py[cod]

# ktlint CLI binary used by the formatting diff harness (downloaded, not committed)
tools/ktlint/

# Left behind by any root-level Bazel invocation.
/bazel-*
/MODULE.bazel.lock
