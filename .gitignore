target/
krusty-out/
docs/badges/
# LLVM source-based coverage counter dumps (see scripts/coverage.sh); can land in the tree root if a
# coverage-instrumented binary runs without LLVM_PROFILE_FILE pointed at target/.
*.profraw
